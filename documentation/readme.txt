to open the documentation in local workstation -> needs to start a localhost
open the batch file named 'local server to Open Documentation InstaMacBot.bat'
it starts the localhost and the documentations is at http://localhost:8080